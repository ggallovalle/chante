settings {
  // NOTE inferred
  // user "$USER"
  paths {
  // home "$HOME"
  // dotfiles "$DOTFILES" or "$HOME/dotfiles"
  // config "$XDG_CONFIG_HOME"

  }
}

packages {
  package "zsh" {
    // NOTE: inferred
    // root zsh
    // config zsh/config/* XDG_CONFIG_HOME/*
    // home zsh/home/* HOME/*
  }
  package "bash"
  package "fish"
  // package "fish"
}

bundles {
  bundle "shells" {
    require "bash"
    require "zsh"
  }
}

